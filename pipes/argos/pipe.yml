name: argos
inputs:
  env:
    required: true
  selectors:
    required: true
    description: Semicolon-separated argos run invocations (pack:… tag:…)
  dash_url:
    description: Dash origin (default https://argos.saidc.ai)
