when:
  - event: [push, manual]
    branch: [dev]

depends_on:
  - ci

labels:
  deploy: true

steps:
  cd:
    image: bash
    environment:
      ENV: office
    commands:
      - bash cicd_acahti/run.sh cd
