when:
  - event: [push, manual]
    branch: [test]

depends_on:
  - ci

labels:
  deploy: true

steps:
  cd:
    image: bash
    environment:
      ENV: hk
    commands:
      - bash cicd_acahti/run.sh cd
